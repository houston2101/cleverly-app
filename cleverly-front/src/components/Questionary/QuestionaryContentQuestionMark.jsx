import styled from '@emotion/styled'

const QuestionaryContentQuestionMark = styled.span`
	font-family: ${({theme}) => theme.typography.fontFamilyLexend};
	color: ${({theme}) => theme.palette.text.placeholder};
	font-size: 15px;
	line-height: 19px;
	text-align: right;
`

export default QuestionaryContentQuestionMark
