import styled from '@emotion/styled'

const TestCartTitle = styled.div`
	font-family: ${({theme}) => theme.typography.fontFamilyLexend};
	color: ${({theme}) => theme.palette.text.secondary};
	font-size: 16px;
	line-height: 20px;
	margin-bottom: auto;
`

export default TestCartTitle
