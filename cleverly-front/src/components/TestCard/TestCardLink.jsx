import styled from '@emotion/styled'
import {Link} from 'react-router-dom'

const TestCardLink = styled(Link)`
	position: absolute;
	width: 100%;
	height: 100%;
	z-index: 2;
	left: 0;
	top: 0;
`

export default TestCardLink
