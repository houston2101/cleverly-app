import styled from '@emotion/styled'

const PageGridTitle = styled.h1`
	font-family: ${({theme}) => theme.typography.fontFamilyLexend};
	font-weight: normal;
	font-size: 40px;
	line-height: 50px;
	color: #333333;
`

export default PageGridTitle
