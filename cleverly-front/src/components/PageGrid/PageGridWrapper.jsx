import styled from '@emotion/styled'
import Container from '../Container'

const PageGridWrapper = styled(Container)`
	display: flex;
	flex-direction: column;
	gap: 65px;
`

export default PageGridWrapper
