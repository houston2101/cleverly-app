import styled from '@emotion/styled'
import {ReactComponent as Bin} from '../../../img/bin.svg'

const DeleteKey = styled(Bin)`
	opacity: 50%;

	&:hover {
		opacity: 100%;
	}
`

export default DeleteKey
