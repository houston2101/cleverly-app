import styled from '@emotion/styled'

const WarningModalText = styled.span`
	color: ${({theme}) => theme.palette.text.secondary};
	font-family: ${({theme}) => theme.typography.fontFamilyLexend};
`

export default WarningModalText
