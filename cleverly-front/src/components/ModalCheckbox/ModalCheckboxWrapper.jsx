import styled from '@emotion/styled'

const ModalCheckboxWrapper = styled.div`
	display: flex;
	position: relative;
	flex-direction: row;
	align-items: center;
	gap: 10px;
	padding-bottom: 15px;

	&:hover {
		cursor: pointer;
	}
`

export default ModalCheckboxWrapper
